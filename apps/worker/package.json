{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/redis": "^4.0.11",
    "redis": "^5.10.0",
    "@repo/db":"*"
  },
  "scripts": {
    "build":"npx tsc -b",
    "start":"node dist/index.js",
    "dev": "bun run build && bun run start"
  }
}
