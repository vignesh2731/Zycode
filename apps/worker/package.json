{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@repo/db": "*",
    "@repo/zod": "*",
    "@types/redis": "^4.0.11",
    "dotenv": "^17.2.3",
    "redis": "^5.10.0"
  },
  "scripts": {
    "build": "npx tsc -b",
    "start": "node dist/index.js",
    "dev": "bun run build && bun run start"
  }
}
